= provide :title, @tag_name
= render 'prototypes/index_header'
.container
  .row
    .tag
      .tag_header
        = @tag_name
      = render partial: 'prototypes/prototype', collection: @prototypes
      = paginate @prototypes
